CC = gcc
CFLAGS = -Wall -Wextra -Werror

TARGET = counter_characts

all: $(TARGET)

$(TARGET): $(TARGET).c
	$(CC) $(CFLAGS) -o $@ $<

clean:
	rm -f $(TARGET)

fclean: clean

re: fclean all